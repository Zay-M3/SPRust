# Assets Folder
This folder contains static assets used by the application.

## Structure:
- `icons/` - Application icons, UI icons
- `fonts/` - Custom fonts (if needed)

## Usage:
Place your image files (PNG, ICO) and font files (TTF, OTF) in the respective folders.
